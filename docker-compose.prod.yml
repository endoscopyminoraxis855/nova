# Production overrides — use with:
#   docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
services:
  frontend:
    build:
      context: ./frontend
      target: prod
    ports:
      - "127.0.0.1:80:80"
    volumes: []   # no hot-reload mounts in prod
